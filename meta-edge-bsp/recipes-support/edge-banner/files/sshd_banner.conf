# EDGE AI OS pre-login banner for SSH. /etc/issue.net is a static file
# rendered at image build by base-files (templates owned by the
# bbappend in meta-edge-distro/recipes-core/base-files/). sshd opens
# it per connection, so it's always current with whatever the image
# shipped — no service, no refresh, just a file.
Banner /etc/issue.net
