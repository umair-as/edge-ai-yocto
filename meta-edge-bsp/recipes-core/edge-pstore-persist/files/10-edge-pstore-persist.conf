# Make systemd-pstore.service wait for the bind-mount onto /data/crash/pstore.
# RequiresMountsFor= auto-pulls in var-lib-systemd-pstore.mount and orders
# this service after it.
[Unit]
RequiresMountsFor=/var/lib/systemd/pstore
