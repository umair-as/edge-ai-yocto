[network]
# CNI is the active network backend. netavark (the podman v5 default) is not
# built — its Rust crate dependency chain is blocked by crates.io UA filtering
# at fetch time. Without this explicit setting podman errors on network ops
# when the netavark binary is absent.
network_backend = "cni"
